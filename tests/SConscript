Import ('env')

name = 'fideo'
inc = env.Dir('.'), env.Dir('../../etilico')
src = env.Glob('*.cpp')
deps = ['fideo-test', 'etilico', 'biopp', 'gmock', 'gtest_main', 'gtest']

env.CreateTest(name, inc, src, deps)
